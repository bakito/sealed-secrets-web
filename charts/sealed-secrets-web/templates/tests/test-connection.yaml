apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sealed-secrets-web.fullname" . }}-test-connection"
  labels:
{{ include "sealed-secrets-web.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sealed-secrets-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
